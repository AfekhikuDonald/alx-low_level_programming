#include <stdio.h>

/**
 * main - prints all single digits number of base 10
 * Return: Always 0(success)
 */
int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
	{
		 putchar(int c);
	}
	putchar("\n");
	return (0);
}
